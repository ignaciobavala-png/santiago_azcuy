// El home es el índice a pantalla completa, que vive en el layout (SiteIndex).
// Esta ruta no agrega contenido debajo: es la "portada" fija del sitio.
export default function HomePage() {
  return null
}
